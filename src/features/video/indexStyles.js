import makeStyles from "@material-ui/styles/makeStyles";

const styles = makeStyles({
  videoSet: {
    marginBottom: 50,
  },
  postControlButton: {
    borderRadius: "50%",
    width: 80,
    height: 80,
    background: "pink",
    marginLeft: "auto",
    marginRight: "auto",
  },
});

export default styles;
